# -*- coding: utf-8 -*-
"""Meta Llama 3 with Hugging Face.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1Hp89YSCH4ibViYTFuBbDG322tiS9VHpu
"""

# !pip install transformers torch bitsandbytes

import torch
from transformers import AutoTokenizer, AutoModelForCausalLM, BitsAndBytesConfig, pipeline

model_id = "meta-llama/Meta-Llama-3.1-8B-Instruct"

tokenizer = AutoTokenizer.from_pretrained(model_id)

from google.colab import userdata
my_secret_key = userdata.get('HF_TOKEN')

bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_use_double_quant=True,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_compute_dtype=torch.bfloat16
)
# "No GPU found" error message

# pip install --upgrade transformers

tokenizer.pad_token = tokenizer.eos_token
model = AutoModelForCausalLM.from_pretrained(model_id,
                                             token=my_secret_key)

text_generator = pipeline(
    "text-generation",
    model = model,
    tokenizer = tokenizer,
    max_new_tokens = 128
)

def get_response(prompt):
  response = text_generator(prompt)
  gen_text = response[0]['generated_text']
  return gen_text

prompt = "Explain the concept of Transformers in LLMs"
llama_response = get_response(prompt)
print(llama_response)